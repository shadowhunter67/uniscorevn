/** Đổi tên sang `InstitutionProfilePage.tsx` — file này chỉ còn re-export để không phá import cũ
 * (nếu có). Import mới nên dùng thẳng `./InstitutionProfilePage`. */
export { InstitutionProfilePage as GenericSchoolEvaluationPage } from './InstitutionProfilePage';
